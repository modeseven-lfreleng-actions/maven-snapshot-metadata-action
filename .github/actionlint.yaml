---
# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: 2025 The Linux Foundation

# Path-specific configurations. None are needed: the self-test calls
# this action as 'uses: $/.', which the actionlint fork validates like
# any local action, inputs included.
paths: {}
